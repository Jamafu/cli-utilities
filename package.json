{
  "name": "git",
  "version": "1.0.0",
  "description": "CLI utilities for better developer experience",
  "author": "jamafu",
  "license": "UNLICENSED",
  "scripts": {
    "build": "tsc",
    "clean-build": "rm -rf _dist && tsc",
    "clean-install": "rm -rf node_modules && npm install",
    "clean": "rm -rf _dist && npm run build",
    "format": "npx biome format --write src"
  },
  "dependencies": {
    "enquirer": "^2.4.1",
    "inversify": "^7.7.0",
    "neverthrow": "^8.2.0",
    "reflect-metadata": "^0.2.2"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.1.4",
    "@types/inquirer": "^9.0.9",
    "@types/node": "^24.2.1",
    "typescript": "^5.5.4"
  },
  "volta": {
    "node": "24.5.0",
    "npm": "10.8.1"
  }
}
